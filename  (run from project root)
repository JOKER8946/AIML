# 1) change to project folder
cd "c:\Users\JOKER\Desktop\AIML"

# 2) init repo if needed
git init

# 3) optional: create .gitignore (good to have)
echo "node_modules/" >> .gitignore
echo ".env" >> .gitignore

# 4) stage and commit
git add .
git commit -m "chore: initial commit"

# 5) set remote (replace <GIT_URL> with the GitHub link)
git remote remove origin 2> /dev/null || true
git remote add origin <GIT_URL>

# 6) ensure branch name (use main or master as needed)
git branch -M main

# 7) authenticate (recommended): use GitHub CLI or set PAT/SSH
#    a) GitHub CLI (interactive)
gh auth login
#    b) or use SSH key and ensure it's added to your GitHub account

# 8) push to GitHub
git push -u origin main

# If push is rejected because remote has history, fetch and rebase:
# git fetch origin
# git pull --rebase origin main
# git push -u origin main
